{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in */src/data/gameData.json) node scripts/generate-master-guide.cjs ;; esac; } 2>/dev/null || true",
            "statusMessage": "Regenerating Master Guide..."
          }
        ]
      }
    ]
  }
}
